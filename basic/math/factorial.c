#include "log.h"

int factorial(int n)
{
    int fact = 0;
    //n! = n * (n-1) * (n-2) .. * 2

    return fact;
}

void main()
{
    int n = 4;
    info("factorial value of %d is %d", n, factorial(n));
}
